# GitMore-v1
Gitmore v1 is the NN model built on ANN - MLP, with the dataset of git commits messages. the dataset is collected from the Cpython open source code to train the model to generate the Git commits learned from data.
